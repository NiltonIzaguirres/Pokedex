import styled from 'styled-components/native';

export const PokemonScreen = styled.SafeAreaView`
  flex: 1;
`;
export const ScrollView = styled.ScrollView.attrs(() => ({
  contentContainerStyle: {
    alignItems: 'center',
  },
}))``;
